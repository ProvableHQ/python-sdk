# aleo-bridge-sdk

Python SDK for bridging assets between Aleo, Ethereum and Solana (Hyperlane warp routes, Circle xReserve).
`pip install aleo-bridge-sdk` → `from aleo_bridge import Bridge`. Expanded in plan 1 Task 12 and plan 4.
